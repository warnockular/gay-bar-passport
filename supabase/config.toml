# Supabase CLI project config placeholder.
# Link this folder to a real project with `supabase link --project-ref <project-ref>`.

project_id = "gay-bar-passport"

[api]
enabled = true
port = 54321
schemas = ["public", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["https://*.vercel.app"]
jwt_expiry = 3600
enable_signup = true
