# Database

Phase 2 starts the Supabase foundation.

- `migrations/001_initial_phase_2_schema.sql` creates the first real schema, indexes, profile trigger, and RLS policies.
- `seed.sql` adds optional public venue placeholders for local development.

Once a Supabase project exists, run the migration through the Supabase SQL editor or the Supabase CLI.
